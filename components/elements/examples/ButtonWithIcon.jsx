var React = require('react');
var Button = require('../Button.jsx');

var instance = (
  <div>
    <Button icon="cloud"> test </Button>
    <Button icon="code"> test </Button>
  </div> );

React.render(instance, mountNode);
